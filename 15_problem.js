// Create an array with the names of all the things you have learned so far. Then, use a for loop to display all the elements of that array as output.

let myArray = ["Variable declaration", "Data types","String concatenation", "ParseInt()", "ParseFloat()", "toFixed()"]

for(i =0; i < myArray.length; i++){
        let Element = myArray[i];
        console.log(Element)
}